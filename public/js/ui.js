(() => {
  const help = document.getElementById('btn-help');
  if (!help) return;
  help.addEventListener('click', () => {
    alert(
`Наведите камеру на фото скульптуры (Чебурашка / Шапокляк–Гена / Волк).

Чебурашка: дождь апельсинов.
Шапокляк: Лариска машет.
Волк: Заяц дразнит, мопед включается по нажатию.

📸 — снимок, 🤳 — селфи.`
    );
  });
})();
